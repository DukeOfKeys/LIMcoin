#include <stdio.h>
#include <math.h>
#include <string.h>
#ifndef UTILS_H
#define UTILS_H
void itoc(int, char*);
int compareZeros(char*, int);
void ltoc(long num, char* in);
char* SHA256(char* );
#endif
